import { HrefDataDirective } from './href-data.directive';

describe('HrefDataDirective', () => {
  it('should create an instance', () => {
    const directive = new HrefDataDirective();
    expect(directive).toBeTruthy();
  });
});
